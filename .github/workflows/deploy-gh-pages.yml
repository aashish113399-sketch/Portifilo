name: Manual gh-pages fallback

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Manual deploy placeholder
        run: echo "This workflow is intentionally disabled; use the GitHub Pages workflow instead."
